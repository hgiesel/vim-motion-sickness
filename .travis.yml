language: ruby
sudo: true
cache: bundler
rvm:
  - 2.3.1
addons:
  apt:
    packages:
      - xvfb
      - vim
      - vim-gtk
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - "vim --version"
